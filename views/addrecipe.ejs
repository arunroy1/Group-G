<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Add Recipe</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap"
    rel="stylesheet"/>
  <link rel="stylesheet" href="/css/styles.css"/>
</head>
<body>
  <div class="container">
    <header class="header-panel">
      <h1>Add a New Recipe</h1>
      <nav class="nav-bar">
        <div class="nav-item"><a href="/">Home</a></div>
        <div class="nav-item"><a href="/recipes">All Recipes</a></div>
        <div class="nav-item"><a href="/recipes/addrecipe">Add Recipe</a></div>
        <div class="nav-item"><a href="/profile">Profile</a></div>
        <!-- removed logout from here -->
      </nav>
    </header>

    <main class="content-box">
      <% if (error) { %>
        <p class="error-message"><%= error %></p>
      <% } %>

      <form
        class="auth-form"
        action="/recipes/addrecipe"
        method="POST"
        enctype="multipart/form-data"
      >
        <label>
          Title *
          <input
            type="text"
            name="title"
            value="<%= form.title %>"
            required />
        </label>
        <label>
          Description *
          <input
            type="text"
            name="description"
            value="<%= form.description %>"
            required />
        </label>
        <label>
          Ingredients (comma separated) *
          <input
            type="text"
            name="ingredients"
            value="<%= form.ingredients %>"
            required />
        </label>
        <label>
          Instructions *
          <textarea
            name="instructions"
            required><%= form.instructions %></textarea>
        </label>
        <label>
          Tags (optional)
          <input
            type="text"
            name="tags"
            value="<%= form.tags %>" />
        </label>
        <label>
          Image (optional)
          <input
            type="file"
            name="image"
            accept="image/*" />
        </label>

        <button type="submit">Save Recipe</button>
      </form>
    </main>

    <footer class="footer">
      &copy; 2025 Recipe Explorer
    </footer>
  </div>

  <script src="/js/main.js"></script>
</body>
</html>